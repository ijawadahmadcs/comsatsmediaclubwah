"use client";

import { FormEvent, useEffect, useState } from "react";

type Admin = { _id: string; registrationNumber: string; name?: string; role: string; isActive: boolean; createdAt: string };

export default function AdminsPage() {
  const [admins, setAdmins] = useState<Admin[]>([]);
  const [form, setForm] = useState({ registrationNumber: "", password: "", name: "" });
  const [message, setMessage] = useState("");
  const [loading, setLoading] = useState(true);
  async function load() { try { const response = await fetch("/api/admins"); const result = await response.json(); if (!response.ok) throw new Error(result.message); setAdmins(result.admins); } catch (error) { setMessage(error instanceof Error ? error.message : "Unable to load administrators."); } finally { setLoading(false); } }
  useEffect(() => { const timer = window.setTimeout(() => { void load(); }, 0); return () => window.clearTimeout(timer); }, []);
  async function create(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/admins", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const result = await response.json(); if (!response.ok) { setMessage(result.message); return; } setForm({ registrationNumber: "", password: "", name: "" }); setMessage("Administrator created."); void load(); }
  async function update(id: string, data: Record<string, unknown>, success: string) { const response = await fetch(`/api/admins/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); const result = await response.json(); setMessage(response.ok ? success : result.message); if (response.ok) void load(); }
  async function remove(admin: Admin) { if (!window.confirm(`Delete ${admin.registrationNumber}?`)) return; const response = await fetch(`/api/admins/${admin._id}`, { method: "DELETE" }); const result = await response.json(); setMessage(response.ok ? "Administrator deleted." : result.message); if (response.ok) void load(); }
  return <main className="min-h-screen bg-[#08090b] px-5 py-16 text-white sm:px-10 lg:px-16"><div className="mx-auto max-w-6xl"><p className="text-xs uppercase tracking-[0.3em] text-blue-300/60">Superadmin · Access</p><h1 className="mt-4 text-4xl font-semibold">Admin Management</h1><p className="mt-3 text-sm text-white/40">Decide who can access the CMS.</p>{message && <p className="mt-5 text-sm text-blue-200">{message}</p>}<form onSubmit={create} className="mt-8 grid gap-4 rounded-2xl border border-white/10 bg-[#0b0d10] p-6 sm:grid-cols-4"><input required placeholder="Registration Number" value={form.registrationNumber} onChange={(event) => setForm({ ...form, registrationNumber: event.target.value })} className="rounded-xl border border-white/10 bg-white/[0.04] px-4 py-3 text-sm outline-none" /><input required minLength={8} type="password" placeholder="Password" value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} className="rounded-xl border border-white/10 bg-white/[0.04] px-4 py-3 text-sm outline-none" /><input placeholder="Name (optional)" value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} className="rounded-xl border border-white/10 bg-white/[0.04] px-4 py-3 text-sm outline-none" /><button className="rounded-xl bg-white px-4 py-3 text-sm font-medium text-black">Add Admin</button></form><div className="mt-8 overflow-x-auto rounded-2xl border border-white/10 bg-[#0b0d10]"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase tracking-[0.15em] text-white/30"><tr><th className="p-4">Name</th><th className="p-4">Registration</th><th className="p-4">Role</th><th className="p-4">Status</th><th className="p-4">Actions</th></tr></thead><tbody>{loading ? <tr><td colSpan={5} className="p-12 text-center text-white/40">Loading administrators...</td></tr> : admins.map((admin) => <tr key={admin._id} className="border-b border-white/[0.06]"><td className="p-4 text-white/80">{admin.name || "-"}</td><td className="p-4 text-white/60">{admin.registrationNumber}</td><td className="p-4 text-white/60">{admin.role}</td><td className="p-4 text-white/60">{admin.isActive ? "Active" : "Inactive"}</td><td className="p-4"><div className="flex flex-wrap gap-2">{admin.role !== "superadmin" && <><button onClick={() => void update(admin._id, { isActive: !admin.isActive }, admin.isActive ? "Administrator deactivated." : "Administrator activated.")} className="rounded-lg border border-white/10 px-3 py-2 text-xs text-white/70">{admin.isActive ? "Deactivate" : "Activate"}</button><button onClick={() => { const password = window.prompt("New password (8+ characters):"); if (password) void update(admin._id, { password }, "Password reset."); }} className="rounded-lg border border-white/10 px-3 py-2 text-xs text-white/70">Reset Password</button><button onClick={() => void remove(admin)} className="rounded-lg border border-red-400/20 px-3 py-2 text-xs text-red-200">Delete</button></>}</div></td></tr>)}</tbody></table></div></div></main>;
}
